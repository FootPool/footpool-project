const initialState = {
  user: {},
  matches: []
};

function fixturesReducer(reduxState = initialState, action) {
  switch (action.type) {
    default:
      return reduxState;
  }
}

export default fixturesReducer;
