const initialState = {
  user: {}
};

function profileReducer(reduxState = initialState, action) {
  switch (action.type) {
    default:
      return reduxState;
  }
}

export default profileReducer;
